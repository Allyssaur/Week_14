import React from 'react';  //Allows simple breaks on the page to appear for some white space - ensures it doesn't require use of other components

const Breaks = () => {
        return (
            <div>
            <br></br>
            <br></br>
            </div>
        )
    }

export default Breaks;